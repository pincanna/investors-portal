<div class="card">
  <h2 class="card-header">Accounts Requiring Verification</h2>
  <div class="card-body">
    <div class="alert alert-info">
      New users are required to obtain a verification code. Please ONLY provide a verification code to the person it was
      intended for.
      <br>
      If a user makes too many failed attempts, their verification code will change.
    </div>
  </div>
  <table class="table table-bordered table-striped table-sm">
    <thead>
      <tr>
        <th>Name</th>
        <th>Verification Code</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.paranoid_verification_code %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>